name:  Test Workflow

on: push

jobs:
   tests:
     runs-on: ubuntu-latest
     steps:
        - name: Checkout
          uses: actions/checkout@v6.0.3

        - name: install dependancies
          run: npm ci

        - name: Run Tests
          run: npm test

